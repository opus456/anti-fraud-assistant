{
  "name": "anti-fraud-gateway",
  "version": "1.0.0",
  "description": "Express + Socket.io gateway for anti-fraud assistant",
  "main": "src/index.js",
  "scripts": {
    "predev": "npx kill-port 3001 || echo port 3001 is already free",
    "prestart": "npx kill-port 3001 || echo port 3001 is already free",
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "http-proxy-middleware": "^3.0.0",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "kill-port": "^2.0.1"
  }
}
