# Docker 运行说明

当前 docker-compose.yml 用于启动基础设施：

- PostgreSQL (pgvector)
- Redis

应用服务按开发模式分别启动：

- server-python (FastAPI)
- server-node (Gateway)
- client-react (Web)

## 1. 启动基础设施

```bash
docker compose up -d
```

## 2. 检查状态

```bash
docker compose ps
docker compose logs -f postgres
docker compose logs -f redis
```

## 3. 停止

```bash
docker compose down
```

## 4. 清理数据卷 (危险)

```bash
docker compose down -v
```

## 环境建议

- PostgreSQL 默认端口: 5432
- Redis 默认端口: 6379
- 数据库初始化脚本: database/init.sql

如需生产一体化部署，建议后续新增专用 compose 文件，将 server-python、server-node、client-react 也容器化并接入同一网络。
