import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.antifraud.assistant',
  appName: 'Anti Fraud Assistant',
  webDir: 'dist'
};

export default config;
